aixvipmap_theme (ownCloud app)
==============================

AixViPMaP theme for ownCloud 10 including style sheets and logos.

Install
-------

Copy `aixvipmap_theme` to ownCloud `apps` directory.
